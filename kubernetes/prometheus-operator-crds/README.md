# prometheus-operator-crds

Terraform module to deploy Prometheus Operator CRDs only.


## Usage

```hcl
module "prometheus_operator_crds" {
  source = "github.com/gigawhat/terraform-modules//kubernetes/prometheus-operator?ref=prometheus-operator-crds-v0.80.0"
}
```
