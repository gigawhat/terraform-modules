# prometheus-operator

Terraform module to deploy Prometheus Operator


## Usage

```hcl
module "prometheus_operator_crds" {
  source = "github.com/gigawhat/terraform-modules//kubernetes/prometheus-operator?ref=prometheus-operator-0.80.0"
}
```
